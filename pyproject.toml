[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lightweight-hbcc"
version = "0.1.0"
description = "Two-session no-augmentation HBCC/Context-Cluster CIFAR pipeline."
requires-python = ">=3.10"
dependencies = [
    "einops",
    "numpy",
    "pandas",
    "pyyaml",
    "torch",
    "torchvision",
    "tqdm",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["lightweight_hbcc*"]
